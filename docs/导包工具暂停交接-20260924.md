# 导包工具暂停交接

**状态：按用户要求于 2026-09-24 暂停后续验证。用户随后授权今日收尾提交与云端推送；本次交付为导包工具首版，未完成全部验收。暂停后未再执行迁移或功能验证。**

## 下次接续先读

1. 本文：当前进度、未完成事项和恢复位置。
2. [验收记录](导包工具验收-20260924.md)：实测结果、限制、执行批次与证据路径。
3. [使用与设计说明](导包工具.md)：操作方式、版本范围、目录结构及迁移保护规则。
4. 项目根目录 `AGENTS.md`：备份、WinUI、日志及提交规范。

工作区：`D:\xinke.wang\项目\ICS\个人工具箱`。

**恢复后不要直接重放已成功的迁入。先检查工作区和目标现状，仅执行仍需要的工作。未经用户要求不推送。**

## 本次交付的实现范围

- WinUI「系统配置 → 导包工具」，已注册侧边栏、仪表盘、导航和 DI；旧 WPF 未修改。
- 源/目标独立连接、类型检索和跨类型多选、依赖树、AML/ZIP 导出、官方包载入、目标差异、迁入、取消与历史。
- Core 的迁移模型及三个接口，Services 的会话、包校验、预检、备份和回读编排。
- net48 官方引擎桥接，隔离主程序 IOM，凭据通过标准输入传递。
- 官方多段 AML、所属 Field 修补、关系 ItemType 去重、命名引用、版本化 ID、多语言处理。
- 同名异 ID、危险字段变更、缺失依赖/语言/资源、删除动作、包损坏和目标并发变化的拦截。
- 执行前目标备份、新增清单、包记录备份、失败后回读和操作/错误日志。
- 使用说明、验收说明及自动回归/原生 WinUI 测试源码。

## 已验证结果

| 类别 | 结果与证据 |
|---|---|
| 主项目构建 | WinUI 正常构建通过，0 错误；net48 桥接存在于输出 `PackageBridge/`，新页面 XBF 已生成。日志：`.codex/package-final-build.log`。 |
| 构建警告 | 2 条 NU1900，原因是本机无法获取 NuGet 漏洞数据；未关闭漏洞检查。 |
| 服务层模拟回归 | 最后一次 **30 项全部通过**。日志：`.codex/package-regression.log`。覆盖依赖循环/去重、关系/字段修补、命名引用、多语言、同名異 ID、重复导入、坏包、并发变化、备份失败、部分失败、取消和凭据脱敏等。 |
| 原生 WinUI | 实际加载页面/XBF；搜索、多选、依赖树、差异选择、进度通知线程、取消入口、历史及计划失效检查通过。`.codex/package-native/package-native.log` 最后一次为 PASS，文件保留了调试阶段的历史失败条目；截图为 `package-page.png`。 |
| JSAB / BLPLM | 均实际返回 `14.0.9.36244`。官方引擎为 `14.0.28.41847`。 |
| 官方互通 | 工具生成的包由官方引擎迁入成功；官方回读快照去掉工具箱 sidecar 后仍能载入、预检，14 项均判为相同。 |
| 源未登记 PackageDefinition 的对象类 | IC_PreviewSetting 可直接选择导出；没有在 JSAB 新建或修改 PackageDefinition。 |
| 实库迁入 | 4 批、14 个根配置入口全部迁入并官方回读一致，详见下一节。 |
| 重复导入保护 | 最终复查 14/14 相同、阻断 0；无需再次写入。记录：`.codex/package-live/evidence/final-validation.txt`。 |
| 临时功能测试 | 临时记录 Preliminary → In Review；新流程进入 Active；窗体及控件元数据可读取。测试记录、流程实例和临时绑定已清理，官方前后快照确认导入的 ItemType 恢复一致。 |
| 凭据 | 本地证据文件扫描匹配连接池 MD5 的数量为 0；凭据未放入进程参数。 |

### BL 中保留的迁入配置

| 批次 | 根配置 | 数量 |
|---|---|---:|
| `run-20260924-155440-c08f24e8` | IC_PreviewSetting 对象类、自动窗体、权限、PresentationConfiguration、CommandBarSection、CommandBarMenuButton、父菜单 | 7 |
| `run-20260924-155934-4d178a63` | IC_Class_Workflow 生命周期及 Preliminary / Released / In Review / Cancel 权限 | 5 |
| `run-20260924-160653-fe3a2cae` | IC_getFormTypeAndId 方法 | 1 |
| `run-20260924-160955-01176381` | AJ_物料发布流程模板 | 1 |

计数不包括所属属性、Body、Field、View、状态、路径、节点和官方包管理记录。上述配置保留，不应作为临时数据清理。

原始 AML、ID、快照、差异与执行日志位于：

```text
.codex/package-live/evidence/EB53BC0808CD03AD/
```

四批成功历史已复制至正常应用输出目录，`result.json` 中的历史目录已更新，可在功能页面查看：

```text
src/ArasToolkit.App.WinUI/bin/Debug/net8.0-windows10.0.19041.0/
  Config/PackageMigration/EB53BC0808CD03AD/run-*/
```

### 临时测试清理记录

- 首次直接测试时，IC_PreviewSetting 没有生命周期和 Allowed Workflow 绑定，服务端拒绝晋升和发起流程；临时记录 `5DB090F35AD641B4983649140E94C526` 已删除，后续查询确认不存在。最初清理日志的 NullReference 来自 IOM 查询不存在记录返回 null，不能解释为仍有残留。
- 第二次先保存官方快照，临时增加测试绑定，再完成晋升和流程发起。
- 已清理记录 `F1DCAE5C09464526A335BB173C2677E9`、Workflow `4C81D4779A724AB986906235F8578C6E`、Workflow Process `A86CF386DB8E4109A2421AE27ED01528`、Allowed Workflow `5C1198FC4F63450EA0806475F01FBC6F`、ItemType Life Cycle `B11B14DD3F694EB395D7491B4693EC57`。
- 证据：`.codex/package-live/evidence/functional-validation.txt`、`functional-fixtures.json`、`functional-before/`、`functional-after/`。

## 未验证、受阻与暂缓项

| 项目 | 当前状态 | 下次条件/动作 |
|---|---|---|
| 关系类完整实库迁入 | 已验证官方导出、多段处理和模拟去重；没有实库成功迁入关系类的记录。IC_Actual_Hours_Comparison / IC_Detailed_Difference_Log 样本被图标 404 阻断。 | 选择资源完整的独立关系样本，或由目标环境部署缺失资源后重新预检。不要删除 AML 引用绕过检查。 |
| 浏览器窗体打开 | 窗体布局/控件元数据回读通过，浏览器视觉与交互未验收；浏览器控制接口连接失败。 | 浏览器可用并登录 BL 后验证打开、显示和编辑。 |
| R37 | 未取得并实测对应独立工具；当前不宣称支持。 | 取得正式工具、增加适配器并实测后再放开。 |
| Grid/TGV 等全部显示配置 | 有类型和依赖处理，实库成功覆盖了 CUI/菜单/PresentationConfiguration，未逐一覆盖所有显示类型。 | 用独立样本逐项验证，保留完整依赖和资源报告。 |
| 缺失前置包 | BOM Life Cycle、IC_Dco1 因 BL 缺少 `com.aras.innovator.solution.PLM` 包登记被阻断。 | 核对目标正式安装状态；不得为了通过检查自动补造前置包。 |
| 含官方删除清理段的包 | AJ_会议纪要流程的依赖集合中有自动窗体 delete / where；首版按规则阻断。 | 若未来扩大支持，应先明确可删除范围及恢复策略；当前不直接迁入。 |
| 方法外部依赖 | 方法内容及绑定可迁移；未证明所有方法的动态引用、外部 DLL 和服务器文件都可用。 | 按具体方法审查/部署，再验证实际运行。 |
| 失败/取消的实库破坏性测试 | 已通过模拟，未在 BL 故意制造部分写入或中断。 | 优先继续使用模拟；若需实库故障测试，选专用测试环境。 |

## 当日收尾记录与接续边界

1. **更新日志已写入并回读确认。** 通过 `IChangelogService.AddEntryAsync()` 将版本从 `1.0.74` 递增到 `1.0.75`，作者为“开发团队”，记录 ID 为 `20096`。日志明确标注首版及暂缓验证范围；本地回执为 `.codex/package-release-receipt.json`。
2. **代码与交接文档纳入本次收尾提交。** 用户已授权推送至 `origin/master`，按项目规范先将 master 合并备份至 develop 并推送 develop，再推送 master。具体提交与云端状态以 Git 记录为准。仅提交本模块及对应文档/测试，原有归档、输出和临时文件留在本地。
3. 正常主程序已构建，但本次没有通过真实运行的普通应用完成全页面人工走查；不要将测试宿主的 PASS 当作全部实际连接交互均已验收。
4. 若恢复时继续修改代码，按本地规范先检查现状并另做备份，避免覆盖暂停期间用户的改动。

## 代码与接续工具

```text
src/ArasToolkit.Core/Models/PackageMigration/
src/ArasToolkit.Core/Interfaces/PackageMigration/
src/ArasToolkit.Services/Services/PackageMigration/
src/ArasToolkit.PackageBridge/
src/ArasToolkit.App.WinUI/ViewModels/PackageMigrationViewModel.cs
src/ArasToolkit.App.WinUI/Views/Config/PackageMigrationPage.xaml[.cs]
tests/ArasToolkit.PackageMigration.RegressionTests/
```

还有已有文件中的少量 DI、导航、菜单、仪表盘、项目引用及解决方案注册改动。

本地实库辅助程序 `.codex/package-live/Program.cs` 通过当前连接池取得凭据，不包含硬编码密码。`finalcheck` 是只读核对；`import`、`functional` 会写目标，**不要在恢复任务时默认运行**。辅助程序中的 `export` 会覆盖它自己的 latest-manifest 指针，下次操作前要核对该指针和预检计划，不要误用上次样本。

运行记录、临时构建和官方 API 检查文件都在 `.codex/`，未加入 Git。不得把官方 DLL、反编译检查文件、DBSeeting.local.json 或凭据放入提交。

## 备份点

- 编码前 HEAD：`c2f4dc7`。
- 命名备份：`backup-before-package-migration-20260924`。
- stash 对象：`591cdb2715af71e210753de0dbdf396db3dce95a`，保留未删除。
- 初始状态、工作区/暂存区 patch：`.codex/backups/package-migration-20260924/`。

该 stash 是编码前快照，**不是暂停时最新功能代码的快照**。最新功能代码和文档以本次收尾提交为恢复点；不要直接应用旧 stash 覆盖现有实现。

下次可使用提示：

> 请从 docs/导包工具暂停交接-20260924.md 接续验证。先读取交接、验收记录和当前 Git 状态，保留已有修改；不要重放已成功迁入的 14 项。首版更新日志已写入 v1.0.75，不要重复写入。根据未验证清单继续；若有新代码修改，先备份，再构建与验证，完成后记录新的更新日志并仅提交本次变更，不自动推送。
