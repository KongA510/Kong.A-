# 导包工具验收 · 2026-09-24

用户已要求暂停后续验证，并随后授权今日收尾提交与云端推送。首版更新日志已通过服务写入 `v1.0.75`；当前状态及待办以 [暂停交接文档](导包工具暂停交接-20260924.md) 为准，以下验证结论没有因收尾发布而扩大。

## 环境和编码备份

- JSAB → BLPLM，实际服务器版本均为 `14.0.9.36244`。
- 官方导包引擎 `14.0.28.41847`，独立 net48 桥接；默认 Merge / Thorough。
- 编码前 HEAD：`c2f4dc7`。保留含未跟踪文件的命名 stash：`backup-before-package-migration-20260924`，对象 `591cdb2715af71e210753de0dbdf396db3dce95a`，使用 `stash apply --index` 恢复，未删除备份。
- 备份状态及 patch 位于 `.codex/backups/package-migration-20260924/`。原有未提交/未跟踪内容未纳入功能提交，旧 WPF 未改动。

## 静态与模拟测试

WinUI 主项目和 net48 桥接编译成功；新页面生成 `PackageMigrationPage.xbf`。构建出现 NU1900：本机无法取得 NuGet 漏洞数据，未关闭漏洞检查。

30 项服务层回归通过。原生 WinUI 宿主实际加载页面，验证搜索、多选、依赖树、差异选择、进度、取消、历史及计划失效；高缩放窗口下使用滚动区域避免配置列表被压没。测试截图和日志位于 `.codex/package-native/`。

这些模拟结果不替代官方引擎的实库验证。没有对 BL 制造破损包、部分写入失败或中途取消；这些破坏性场景由模拟测试覆盖。

## 实库迁入结果

4 次迁入共 **14 个根配置入口**，全部通过官方回读。计数不包括所属字段、Body、View、状态、节点、路径以及官方建立的 PackageDefinition 等管理记录。

| 执行 | 配置 | 根配置数 | 结果 |
|---|---|---:|---|
| `run-20260924-155440-c08f24e8` | IC_PreviewSetting 对象类、自动窗体、权限、PresentationConfiguration、CommandBarSection、CommandBarMenuButton、父菜单 | 7 | 成功 |
| `run-20260924-155934-4d178a63` | IC_Class_Workflow 生命周期及 Preliminary / Released / In Review / Cancel 权限 | 5 | 成功 |
| `run-20260924-160653-fe3a2cae` | IC_getFormTypeAndId 方法 | 1 | 成功 |
| `run-20260924-160955-01176381` | AJ_物料发布流程 Workflow Map | 1 | 成功 |

首轮选择依据：IC_PreviewSetting 在 BL 不存在，JSAB 源 PackageDefinition 清单中未登记该对象类；依赖较少，图片及样式在目标可用。实测直接按元素导出成功，没有在 JSAB 建立导包定义。

完整根配置及所属元素、稳定 ID、原始 AML、备份和执行日志保留在本地 `.codex/package-live/evidence/EB53BC0808CD03AD/`。成功记录同时同步到应用 `Config/PackageMigration/`，可通过执行历史打开。原始内容未加入 Git。

重复比较验证相同配置跳过，未进行不必要的再次写入。官方导出的目标快照（只有 imports.mf 与 AML，没有工具箱 sidecar）也通过工具箱读取和比较，验证官方格式互通。

所有任务文件检查未发现连接池 MD5 凭据；凭据没有作为进程参数传递。全局默认 Aras 连接没有被切换。

## 功能操作

迁入的 IC_PreviewSetting 原本没有生命周期和 Allowed Workflow 绑定。首次直接尝试得到“当前状态为空”和“不在允许工作流中”的服务端拒绝，临时记录已删除并再次确认不存在。

随后为本次测试临时创建两个明确 ID 的绑定，先保存官方 ItemType 快照，再用一条禁用状态的临时记录验证：

- 状态由 Preliminary 晋升到 **In Review**。
- 新工作流实例进入 **Active**。
- 窗体及 Body / Field 控件元数据可回读。
- 删除本次 Workflow 关联、Workflow Process、临时记录和两个测试绑定；逐项回读确认不存在。官方导出前后对比确认 ItemType 配置恢复一致，导入的 14 项配置保留。

记录：`.codex/package-live/evidence/functional-validation.txt`；测试绑定 ID 存在同目录 `functional-fixtures.json`。创建、修改和清理均使用正常 Aras API 并记录操作日志，没有直接修改 Aras SQL 表。

## 阻断样本和未完成项

| 场景 | 发现与处理 |
|---|---|
| IC_Actual_Hours_Comparison 和 IC_Detailed_Difference_Log 关系类 | 官方导出完成，包含关系 ItemType、属性、窗体及共享显示配置；目标两个自定义图标返回 404，未迁入。关系类的多段打包/去重通过模拟验证，尚无完整关系类实库迁入成功记录。 |
| BOM Life Cycle、IC_Dco1 | BL 没有 `com.aras.innovator.solution.PLM` 前置包登记，预检阻断；没有自动补造前置包。 |
| AJ_会议纪要流程的依赖集合 | 官方 AML 包含自动窗体清理的 delete / where 操作；首版禁止删除目标配置，阻断。 |
| 浏览器中的窗体打开 | 浏览器控制接口返回连接失败，未完成浏览器视觉/交互验收；不能用控件元数据回读代替此项。 |
| R37 | 尚未取得并验证对应独立工具；不宣称支持。 |
| DLL、图片、Vault 和方法动态引用 | 未自动部署或证明任意方法的外部依赖完整；必要资源缺失仍需目标侧部署后重跑预检。 |

上述限制保留在页面和文档中。配置快照不是整库回滚；需恢复时还要核对新增配置、包管理记录及外部副作用。
