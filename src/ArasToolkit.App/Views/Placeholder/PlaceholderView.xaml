<UserControl x:Class="ArasToolkit.App.Views.Placeholder.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32,28">

            <!-- 标题区域 -->
            <TextBlock x:Name="TitleText" Text="功能开发中"
                       FontSize="26" FontWeight="Bold"
                       Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,4" />
            <TextBlock x:Name="DescriptionText" Text="此功能正在开发中，敬请期待..."
                       FontSize="14"
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,24" />

            <!-- 无子功能时的占位图标 -->
            <StackPanel x:Name="EmptyPlaceholder" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="🚧" FontSize="64" HorizontalAlignment="Center" Margin="0,0,0,16"
                           Foreground="{x:Null}" />
                <TextBlock Text="Aras 开发工具箱"
                           FontSize="13"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center" />
            </StackPanel>

            <!-- 子功能卡片列表（与主仪表盘相同风格：3列WrapPanel + emoji图标） -->
            <ItemsControl x:Name="ChildItemsControl" Visibility="Collapsed">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}" Width="260" Margin="0,0,14,14"
                                Padding="20,18" Cursor="Hand"
                                MouseLeftButtonDown="ChildCard_Click">
                            <StackPanel>
                                <TextBlock Text="{Binding CardIcon}" FontSize="28" Margin="0,0,0,10"
                                           Foreground="{x:Null}" />
                                <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="SemiBold"
                                           Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,4" />
                                <TextBlock Text="{Binding Description}" FontSize="12"
                                           Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </ScrollViewer>
</UserControl>
