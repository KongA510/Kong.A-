<Window x:Class="ArasToolkit.App.Views.TextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="保存模板" Height="200" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="请输入模板名称:" FontSize="13" Margin="0,0,0,10"/>
        <TextBox Grid.Row="1" x:Name="TemplateNameBox" Height="30" FontSize="14"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="取消" Width="70" Height="28" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="确定" Width="70" Height="28" Background="#6366F1" Foreground="White" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
