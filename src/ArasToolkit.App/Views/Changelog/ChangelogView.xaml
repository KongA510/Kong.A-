<UserControl x:Class="ArasToolkit.App.Views.ChangelogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:ArasToolkit.App.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="Transparent">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
        <converters:StringToVisibilityConverter x:Key="StringToVisibility" />

        <!-- 更新类型颜色 -->
        <SolidColorBrush x:Key="NewTypeBrush" Color="#10B981" />
        <SolidColorBrush x:Key="FixTypeBrush" Color="#F59E0B" />
        <SolidColorBrush x:Key="OptimizeTypeBrush" Color="#3B82F6" />
        <SolidColorBrush x:Key="RemoveTypeBrush" Color="#EF4444" />

        <!-- 类型标签颜色转换器 -->
        <Style x:Key="TypeBadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Padding" Value="8,3" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Type}" Value="新增">
                    <Setter Property="Background" Value="#1A10B981" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Type}" Value="修复">
                    <Setter Property="Background" Value="#1AF59E0B" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Type}" Value="优化">
                    <Setter Property="Background" Value="#1A3B82F6" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Type}" Value="移除">
                    <Setter Property="Background" Value="#1AEF4444" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TypeTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Type}" Value="新增">
                    <Setter Property="Foreground" Value="{StaticResource SuccessBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Type}" Value="修复">
                    <Setter Property="Foreground" Value="{StaticResource WarningBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Type}" Value="优化">
                    <Setter Property="Foreground" Value="#3B82F6" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Type}" Value="移除">
                    <Setter Property="Foreground" Value="{StaticResource ErrorBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="32,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="更新日志" FontSize="26" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}" />
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <TextBlock Text="当前版本：" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" />
                    <TextBlock Text="{Binding CurrentVersion}" FontSize="14" FontWeight="Bold"
                               Foreground="{StaticResource AccentBrush}" Margin="4,0,0,0" />
                </StackPanel>
            </StackPanel>
            <Button Grid.Column="1" Content="刷新" Command="{Binding RefreshCommand}"
                    Style="{StaticResource PrimaryButton}" Height="36" FontSize="13"
                    VerticalAlignment="Bottom" Padding="20,8" />
        </Grid>

        <!-- 统计卡片 -->
        <WrapPanel Grid.Row="1" Margin="0,0,0,16">
            <Border Style="{StaticResource CardStyle}" Width="160" Margin="0,0,12,8" Padding="16,14">
                <StackPanel>
                    <TextBlock Text="总计" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                    <TextBlock Text="{Binding TotalCount}" FontSize="24" FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardStyle}" Width="160" Margin="0,0,12,8" Padding="16,14">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="8" Height="8" Fill="{StaticResource SuccessBrush}" Margin="0,0,6,0" VerticalAlignment="Center" />
                        <TextBlock Text="新增" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                    </StackPanel>
                    <TextBlock Text="{Binding NewCount}" FontSize="24" FontWeight="Bold"
                               Foreground="{StaticResource SuccessBrush}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardStyle}" Width="160" Margin="0,0,12,8" Padding="16,14">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="8" Height="8" Fill="{StaticResource WarningBrush}" Margin="0,0,6,0" VerticalAlignment="Center" />
                        <TextBlock Text="修复" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                    </StackPanel>
                    <TextBlock Text="{Binding FixCount}" FontSize="24" FontWeight="Bold"
                               Foreground="{StaticResource WarningBrush}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardStyle}" Width="160" Margin="0,0,12,8" Padding="16,14">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="8" Height="8" Fill="#3B82F6" Margin="0,0,6,0" VerticalAlignment="Center" />
                        <TextBlock Text="优化" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                    </StackPanel>
                    <TextBlock Text="{Binding OptimizeCount}" FontSize="24" FontWeight="Bold"
                               Foreground="#3B82F6" Margin="0,4,0,0" />
                </StackPanel>
            </Border>
        </WrapPanel>

        <!-- 过滤工具栏 -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="16,12" Margin="0,0,0,12">
            <WrapPanel>
                <TextBlock Text="筛选类型：" VerticalAlignment="Center"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="13" Margin="0,0,12,0" />
                <ListBox ItemsSource="{Binding FilterTypes}"
                         SelectedItem="{Binding FilterType}"
                         Background="Transparent" BorderThickness="0">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Margin" Value="0,0,8,0" />
                            <Setter Property="Padding" Value="14,6" />
                            <Setter Property="Cursor" Value="Hand" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="FilterBorder"
                                                Background="{StaticResource SurfaceBrush}"
                                                CornerRadius="6"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="FilterBorder" Property="Background" Value="{StaticResource AccentBrush}" />
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="FilterBorder" Property="Background" Value="{StaticResource AccentHoverBrush}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>

                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"
                           Margin="16,0,0,0" />
            </WrapPanel>
        </Border>

        <!-- 日志列表 -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Entries}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}" 
                                Margin="0,0,0,10" Padding="20,16"
                                Cursor="Hand">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- 左侧时间线标记 -->
                                <Grid Grid.Column="0" Margin="0,0,20,0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <!-- 版本节点 -->
                                    <Border Grid.Row="0" Width="44" Height="44" CornerRadius="22"
                                            Background="{StaticResource AccentBrush}"
                                            HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Version, StringFormat='v{0}'}" FontSize="9" FontWeight="Bold"
                                                   Foreground="White" TextTrimming="CharacterEllipsis"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>

                                    <!-- 时间线 -->
                                    <Border Grid.Row="1" Width="2"
                                            Background="{StaticResource BorderBrush}"
                                            HorizontalAlignment="Center" Margin="0,6,0,0" />
                                </Grid>

                                <!-- 右侧内容 -->
                                <StackPanel Grid.Column="1">
                                    <!-- 日期和版本 -->
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                        <TextBlock Text="📅" FontSize="13" VerticalAlignment="Center" Margin="0,0,6,0" />
                                        <TextBlock Text="{Binding DisplayDate}" FontSize="14" FontWeight="SemiBold"
                                                   Foreground="{StaticResource AccentBrush}"
                                                   VerticalAlignment="Center" />
                                        <TextBlock Text=" · " Foreground="{StaticResource BorderBrush}" FontSize="14" Margin="6,0" />
                                        <TextBlock Text="{Binding Version, StringFormat='v{0}'}" FontSize="13"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   VerticalAlignment="Center" />
                                    </StackPanel>

                                    <!-- 类型标签 -->
                                    <Border Style="{StaticResource TypeBadgeStyle}" Margin="0,0,0,10">
                                        <TextBlock Text="{Binding Type}" Style="{StaticResource TypeTextStyle}" />
                                    </Border>

                                    <!-- 描述 -->
                                    <TextBlock Text="{Binding Description}" FontSize="14"
                                               Foreground="{StaticResource TextPrimaryBrush}"
                                               TextWrapping="Wrap" LineHeight="22" />

                                    <!-- 作者 -->
                                    <TextBlock Text="{Binding Author, StringFormat='—— {0}'}" FontSize="12"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               Margin="0,6,0,0" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
