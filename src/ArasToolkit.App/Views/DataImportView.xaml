<UserControl x:Class="ArasToolkit.App.Views.DataImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:ArasToolkit.App.Converters"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="Transparent">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
        <converters:InverseBoolConverter x:Key="InverseBool" />
        <converters:StringToVisibilityConverter x:Key="StringToVisibility" />
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="数据汇入" FontSize="20" FontWeight="Bold"
                       Foreground="{StaticResource TextPrimaryBrush}" />
            <TextBlock Text="Excel数据导入Aras系统" FontSize="13"
                       Foreground="{StaticResource TextSecondaryBrush}" />
        </StackPanel>

        <!-- 文件选择 + 范围设置 -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="16,12" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="文件: " FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBlock Text="{Binding FileName}" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" />
                        <Button Content="浏览" Command="{Binding BrowseFileCommand}"
                                Style="{StaticResource PrimaryButton}" Height="28" FontSize="12"
                                Padding="12,0" Margin="8,0,0,0" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Sheet:" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" />
                        <ComboBox ItemsSource="{Binding SheetNames}" SelectedItem="{Binding SelectedSheetName}"
                                  Width="140" Margin="6,0,12,0" />
                        <TextBlock Text="起始行:" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBox Text="{Binding StartRow}" Width="50" Margin="4,0" />
                        <TextBlock Text="结束行:" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBox Text="{Binding EndRow}" Width="50" Margin="4,0" />
                        <TextBlock Text="起始列:" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBox Text="{Binding StartCol}" Width="50" Margin="4,0" />
                        <TextBlock Text="结束列:" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" />
                        <TextBox Text="{Binding EndCol}" Width="50" Margin="4,0" />
                        <Button Content="加载预览" Command="{Binding LoadPreviewCommand}"
                                Style="{StaticResource PrimaryButton}" Height="28" FontSize="12"
                                Padding="12,0" Margin="8,0,0,0" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 列映射表 -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="16,8" Margin="0,0,0,8"
                Visibility="{Binding HasColumnMappings, Converter={StaticResource BoolToVisibility}}">
            <StackPanel>
                <TextBlock Text="列映射表 (@A @B ... → 真实列头)" FontSize="12"
                           Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
                <ItemsControl ItemsSource="{Binding ColumnMappings}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Margin="0,0,16,2" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}">
                                <Run Text="{Binding Letter}" FontWeight="Bold" />
                                <Run Text=" = " />
                                <Run Text="{Binding Header}" />
                            </TextBlock>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- ===== Row 3: 数据预览 + AML 编辑器（左右分栏） ===== -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="4*" />
            </Grid.ColumnDefinitions>

            <!-- 左：数据预览 -->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Padding="0">
                <Grid>
                    <DataGrid ItemsSource="{Binding PreviewData}"
                              AutoGenerateColumns="True"
                              IsReadOnly="True" />
                    <TextBlock Text="请先加载预览" FontSize="14"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Visibility="{Binding PreviewData, Converter={StaticResource StringToVisibility}, ConverterParameter=Invert}" />

                        </Grid>
            </Border>

            <!-- 右：AML 编辑器 + 预览 -->
            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Padding="16,14">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" /></Grid.RowDefinitions>

                    <!-- 工具栏 -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="AML 模板" FontSize="13" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   VerticalAlignment="Center" />
                        <Button Content="选择配置" Command="{Binding OpenConfigSelectorCommand}"
                                Height="28" FontSize="11" Padding="10,0" Margin="12,0,0,0" />
                        <Button Content="保存" Command="{Binding SaveConfigCommand}"
                                Height="28" FontSize="11" Padding="10,0" Margin="6,0,0,0" />
                        <Button Content="预览" Command="{Binding PreviewAmlCommand}"
                                Height="28" FontSize="11" Padding="10,0" Margin="6,0,0,0" />
                    </StackPanel>

                    <TextBox Grid.Row="1" Text="{Binding AmlContent, UpdateSourceTrigger=PropertyChanged}"
                             TextWrapping="Wrap" AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas" FontSize="12"
                             Background="{StaticResource SurfaceBrush}"
                             Foreground="{StaticResource TextPrimaryBrush}"
                             BorderBrush="{StaticResource BorderBrush}"
                             BorderThickness="1" Padding="10" MinHeight="100" />

                    <TextBlock Grid.Row="2" Text="AML 预览" FontSize="12" FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,10,0,4"
                               Visibility="{Binding PreviewResult, Converter={StaticResource StringToVisibility}}" />

                    <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto"
                                  Visibility="{Binding PreviewResult, Converter={StaticResource StringToVisibility}}">
                        <Border Background="#F0F9FF" Padding="10" CornerRadius="4">
                            <TextBlock Text="{Binding PreviewResult}" FontSize="11"
                                       FontFamily="Consolas" TextWrapping="Wrap" />
                        </Border>
                    </ScrollViewer>

                    <!-- 执行按钮行 -->
                    <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="执行导入" Command="{Binding ExecuteImportCommand}"
                                Style="{StaticResource PrimaryButton}" Height="32" FontSize="13" Padding="20,0"
                                Visibility="{Binding IsImporting, Converter={StaticResource BoolToVisibility}, ConverterParameter=Invert}" />
                        <Button Content="暂停" Command="{Binding PauseCommand}"
                                Height="32" FontSize="13" Padding="16,0" Margin="8,0,0,0"
                                Background="#F59E0B" Foreground="White" BorderThickness="0" Cursor="Hand"
                                Visibility="{Binding IsPaused, Converter={StaticResource BoolToVisibility}, ConverterParameter=Invert}" />
                        <Button Content="继续" Command="{Binding ResumeCommand}"
                                Style="{StaticResource PrimaryButton}" Height="32" FontSize="13" Padding="16,0" Margin="8,0,0,0"
                                Visibility="{Binding IsPaused, Converter={StaticResource BoolToVisibility}}" />
                    </StackPanel>

                    <!-- 进度条行 -->
                    <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,8,0,0"
                                Visibility="{Binding IsProgressVisible, Converter={StaticResource BoolToVisibility}}">
                        <ProgressBar Value="{Binding ImportProgress}" Minimum="0" Maximum="100" Height="6" Width="180"
                                     Foreground="{StaticResource AccentBrush}" Background="{StaticResource BorderBrush}" BorderThickness="0" />
                        <TextBlock Text="{Binding ProgressText}" FontSize="11"
                                   Foreground="{StaticResource TextSecondaryBrush}" Margin="12,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
<!-- 状态 -->
        <Border Grid.Row="4" Padding="0,4">
            <StackPanel>
                <Border Background="#3FEF4444" CornerRadius="4" Padding="8,4"
                        Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibility}}"
                        Margin="0,0,0,4">
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}" FontSize="12" />
                </Border>
                <TextBlock Text="{Binding StatusMessage}" FontSize="12"
                           Foreground="{StaticResource TextSecondaryBrush}" />
                <TextBlock Text="{Binding LastResult.LogFilePath, StringFormat='日志: {0}'}" FontSize="11"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Visibility="{Binding HasResult, Converter={StaticResource BoolToVisibility}}" />
                <ProgressBar Style="{StaticResource DarkProgressBar}"
                             Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"
                             Margin="0,4,0,0" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
