<Window x:Class="ArasToolkit.App.Views.ArasLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:ArasToolkit.App.Converters"
        Title="Aras 登录信息管理"
        Height="520" Width="560"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="{StaticResource PrimaryBrush}"
        ResizeMode="NoResize">

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
        <converters:StringToVisibilityConverter x:Key="StringToVisibility" />
        <converters:MultiStringEqualityConverter x:Key="MultiStringEquality" />
        <converters:InverseBoolConverter x:Key="InverseBool" />

        <Style TargetType="Button" x:Key="WindowButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
        </Style>
        <Style TargetType="Button" x:Key="ConnectBtn">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
        </Style>
        <Style TargetType="Button" x:Key="DeleteBtn">
            <Setter Property="Background" Value="#EF4444"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="Button" x:Key="EditBtn">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <!-- 表单标签样式 -->
        <Style TargetType="TextBlock" x:Key="FormLabel">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="MinWidth" Value="50"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="8" Background="{StaticResource PrimaryBrush}"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Aras 登录信息管理" FontSize="18" FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}" />
                    <TextBlock Text="查看和管理已保存的 Aras 连接配置" FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                    <Button Content="＋ 新增" Style="{StaticResource ConnectBtn}" Height="28" FontSize="12" Padding="10,0" Margin="0,0,8,0"
                            Command="{Binding ToggleFormCommand}" />
                    <Button Content="✕" Style="{StaticResource WindowButton}" Click="CloseButton_Click" />
                </StackPanel>
            </Grid>

            <!-- 新增/编辑表单（可折叠） -->
            <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="16,14" Margin="0,0,0,12"
                    Visibility="{Binding IsFormVisible, Converter={StaticResource BoolToVisibility}}">
                <StackPanel>
                    <TextBlock Text="{Binding FormTitle}" FontSize="14" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12" />

                    <!-- URL -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="URL:" Style="{StaticResource FormLabel}" />
                        <TextBox Grid.Column="1" Text="{Binding FormUrl, UpdateSourceTrigger=PropertyChanged}"
                                 FontSize="13" Padding="8,6"
                                 Style="{StaticResource DarkTextBox}" />
                    </Grid>

                    <!-- 数据库 -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="数据库:" Style="{StaticResource FormLabel}" />
                        <TextBox Grid.Column="1" Text="{Binding FormDatabase, UpdateSourceTrigger=PropertyChanged}"
                                 FontSize="13" Padding="8,6"
                                 Style="{StaticResource DarkTextBox}" />
                    </Grid>

                    <!-- 用户名 -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="用户名:" Style="{StaticResource FormLabel}" />
                        <TextBox Grid.Column="1" Text="{Binding FormUsername, UpdateSourceTrigger=PropertyChanged}"
                                 FontSize="13" Padding="8,6"
                                 Style="{StaticResource DarkTextBox}" />
                    </Grid>

                    <!-- 密码 -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="密码:" Style="{StaticResource FormLabel}" />
                        <PasswordBox Grid.Column="1" x:Name="NewPasswordBox"
                                     FontSize="13" Padding="8,6" />
                        <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="8,0,0,0">
                            <Button Content="保存" Command="{Binding SaveCommand}"
                                    Style="{StaticResource ConnectBtn}" Height="28" FontSize="12" Padding="12,0" />
                            <Button Content="取消" Command="{Binding CancelEditCommand}"
                                    Style="{StaticResource EditBtn}" Height="28" FontSize="12" Padding="12,0" Margin="4,0,0,0" />
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 连接列表 -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding LoginInfos}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource CardBrush}"
                                    BorderBrush="{StaticResource BorderBrush}"
                                    BorderThickness="1" CornerRadius="8"
                                    Padding="14,12" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock FontSize="14" FontWeight="SemiBold"
                                                       Foreground="{StaticResource TextPrimaryBrush}">
                                                <Run Text="{Binding Username}" />
                                                <Run Text="@" Foreground="{StaticResource TextSecondaryBrush}" />
                                                <Run Text="{Binding Database}" Foreground="{StaticResource AccentBrush}" />
                                            </TextBlock>
                                            <!-- 已启用标记 -->
                                            <Border CornerRadius="3" Padding="6,2" Margin="8,0,0,0"
                                                    Background="#DEF7EC"
                                                    Visibility="{Binding IsEnabled, Converter={StaticResource BoolToVisibility}}">
                                                <TextBlock Text="✓ 已启用" FontSize="10"
                                                           Foreground="#16A34A" FontWeight="SemiBold" />
                                            </Border>
                                        </StackPanel>
                                        <TextBlock Text="{Binding Url}" FontSize="11"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   TextTrimming="CharacterEllipsis" Margin="0,4,0,0" />
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal"
                                                VerticalAlignment="Center">
                                        <Button Content="编辑" Style="{StaticResource EditBtn}"
                                                Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}" />
                                        <Button Style="{StaticResource ConnectBtn}"
                                                Command="{Binding DataContext.ConnectCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}" Margin="4,0,0,0"
                                                IsEnabled="{Binding DataContext.IsProcessing, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBool}, FallbackValue=True}">
                                            <Button.Content>
                                                <TextBlock>
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Text" Value="连接" />
                                                            <Style.Triggers>
                                                                <DataTrigger Value="True">
                                                                    <DataTrigger.Binding>
                                                                        <MultiBinding Converter="{StaticResource MultiStringEquality}">
                                                                            <Binding Path="Id" />
                                                                            <Binding Path="DataContext.ConnectingId" RelativeSource="{RelativeSource AncestorType=Window}" />
                                                                        </MultiBinding>
                                                                    </DataTrigger.Binding>
                                                                    <Setter Property="Text" Value="连接中..." />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Button.Content>
                                        </Button>
                                        <Button Content="删除" Style="{StaticResource DeleteBtn}"
                                                Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding Id}"
                                                Margin="4,0,0,0" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- 状态栏 -->
            <Border Grid.Row="3" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,1,0,0" Padding="0,12,0,0">
                <StackPanel>
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}"
                               FontSize="12" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibility}}" />
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource TextSecondaryBrush}"
                               FontSize="12" Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisibility}}" />
                    <ProgressBar Style="{StaticResource DarkProgressBar}"
                                 Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibility}}"
                                 Margin="0,6,0,0" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
