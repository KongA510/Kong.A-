<UserControl x:Class="ArasToolkit.App.Views.FileExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:ArasToolkit.App.Converters"
             xmlns:models="clr-namespace:ArasToolkit.Core.Models;assembly=ArasToolkit.Core"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1000"
             Background="Transparent">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
        <converters:InverseBoolConverter x:Key="InverseBool" />
        <converters:StringToVisibilityConverter x:Key="StringToVisibility" />
        <converters:StringToColorConverter x:Key="StringToColor" />
        <converters:StringToColorBrushConverter x:Key="StringToColorBrush" />
        <converters:EnterTooltipConverter x:Key="EnterTooltipConverter" />

        <!-- 上下文菜单按钮样式 -->
        <Style x:Key="ContextMenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Padding" Value="6,3" />
            <Setter Property="Margin" Value="4,0" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource SystemAccentBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 文件/文件夹项样式 -->
        <Style x:Key="FileItemBorder" TargetType="Border">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="12,5" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Margin" Value="0" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <DataTemplate DataType="{x:Type models:FileSystemItem}">
            <Border Style="{StaticResource FileItemBorder}"
                    MouseLeftButtonDown="Item_MouseLeftButtonDown"
                    Tag="{Binding}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="36" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" MinWidth="80" />
                        <ColumnDefinition Width="Auto" MinWidth="140" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- 彩色图标圆底 -->
                    <Border Grid.Column="0" Width="28" Height="28" CornerRadius="14"
                            VerticalAlignment="Center" HorizontalAlignment="Center"
                            Background="{Binding IconColor, Converter={StaticResource StringToColorBrush}}">
                        <TextBlock Text="{Binding Icon}" FontSize="15"
                                   VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Border>

                    <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="13"
                               VerticalAlignment="Center" TextTrimming="CharacterEllipsis"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                               Margin="4,0,0,0" />

                    <TextBlock Grid.Column="2" Text="{Binding SizeDisplay}" FontSize="12"
                               VerticalAlignment="Center" HorizontalAlignment="Right"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               Margin="0,0,16,0" />

                    <TextBlock Grid.Column="3" Text="{Binding LastModifiedDisplay}" FontSize="12"
                               VerticalAlignment="Center" HorizontalAlignment="Right"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

                    <!-- 操作按钮：在资源管理器中定位/打开 -->
                    <Button Grid.Column="4" Content="📂"
                            ToolTip="{Binding Converter={StaticResource EnterTooltipConverter}}"
                            Click="OpenInExplorerButton_Click"
                            Tag="{Binding}"
                            Style="{StaticResource ContextMenuButtonStyle}"
                            Width="26" Height="26" FontSize="13" />
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="📁 我的资料" FontSize="24" FontWeight="Bold"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            <TextBlock Text="文件资源管理器 — 浏览、搜索、上传、打开文件"
                       FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       Margin="0,4,0,0" />
        </StackPanel>

        <!-- 导航栏 -->
        <Border Grid.Row="1" Style="{StaticResource SimpleCard}" Padding="12,10" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="⬅ 返回" Command="{Binding NavigateBackCommand}"
                        Style="{StaticResource PageBtn}" Width="70" Margin="0,0,8,0" />

                <Border Grid.Column="2" Background="{DynamicResource ControlFillColorDefaultBrush}"
                        CornerRadius="4" Padding="10,5" VerticalAlignment="Center">
                    <TextBlock Text="{Binding BreadcrumbPath}" FontSize="12"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                               TextTrimming="CharacterEllipsis" />
                </Border>

                <StackPanel Grid.Column="3" Orientation="Horizontal" Margin="8,0,0,0">
                    <Button Content="📤 上传" Command="{Binding UploadFileCommand}"
                            Style="{StaticResource FluentButton}" Height="32" FontSize="12"
                            Padding="10,0" Margin="0,0,4,0" />
                    <Button Content="🔄" Command="{Binding RefreshCommand}"
                            Style="{StaticResource IconBtn}" Width="32" Height="32"
                            FontSize="14" Margin="0,0,4,0" />
                    <Button Content="📂" Command="{Binding BrowseFolderCommand}"
                            Style="{StaticResource IconBtn}" Width="32" Height="32"
                            FontSize="14" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- 搜索栏 -->
        <Border Grid.Row="2" Style="{StaticResource SimpleCard}" Padding="12,10" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="🔍" FontSize="16"
                           VerticalAlignment="Center" Margin="0,0,8,0" />

                <TextBox Grid.Column="1"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="13" Padding="8,6"
                         BorderThickness="0">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource FluentTextBox}">
                            <Setter Property="BorderThickness" Value="0" />
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Button Grid.Column="2" Content="✕" FontSize="12"
                        Style="{StaticResource IconBtn}" Width="28" Height="28"
                        Visibility="{Binding IsSearchActive, Converter={StaticResource BoolToVisibility}}"
                        Command="{Binding RefreshCommand}" />

                <TextBlock Grid.Column="1" Text="搜索文件..." FontSize="13"
                           Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                           IsHitTestVisible="False"
                           Visibility="{Binding SearchText, Converter={StaticResource StringToVisibility}, ConverterParameter=Invert}"
                           Margin="8,0,0,0" VerticalAlignment="Center" />
            </Grid>
        </Border>

        <!-- 文件列表卡片 -->
        <Border Grid.Row="3" Style="{StaticResource SimpleCard}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- 列标题 -->
                <Border Grid.Row="0" Padding="12,8" Background="{DynamicResource ControlFillColorTertiaryBrush}"
                        BorderBrush="{DynamicResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="36" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" MinWidth="80" />
                            <ColumnDefinition Width="Auto" MinWidth="140" />
                            <ColumnDefinition Width="Auto" MinWidth="72" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" />
                        <TextBlock Grid.Column="1" Text="名称" FontSize="12" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                        <TextBlock Grid.Column="2" Text="大小" FontSize="12" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   HorizontalAlignment="Right" Margin="0,0,16,0" />
                        <TextBlock Grid.Column="3" Text="修改时间" FontSize="12" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   HorizontalAlignment="Right" />
                        <TextBlock Grid.Column="4" Text="操作" FontSize="12" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   HorizontalAlignment="Center" Margin="8,0,0,0" />
                    </Grid>
                </Border>

                <!-- 文件列表 -->
                <ListBox Grid.Row="1" ItemsSource="{Binding CurrentItems}"
                         BorderThickness="0" Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         VirtualizingPanel.ScrollUnit="Pixel">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>

                <ProgressBar Grid.Row="1" IsIndeterminate="True" Height="3"
                             Style="{StaticResource FluentProgressBar}"
                             Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"
                             VerticalAlignment="Top" />

                <TextBlock x:Name="EmptyLabel" Grid.Row="1" Text="此文件夹为空"
                           FontSize="14" Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Visibility="Collapsed">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentItems.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>

        <!-- 状态栏 -->
        <Border Grid.Row="4" Margin="0,10,0,0" Padding="0,4,0,0"
                BorderBrush="{DynamicResource DividerStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0">
            <TextBlock Text="{Binding StatusMessage}" FontSize="12"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        </Border>
    </Grid>
</UserControl>
